Castle Windsor Lifestyle implementation that renews (recreates) a shared instance of a component at specified intervals.



TODO:

- Will previous instances be released from container and disposed immediately upon renewal or will they remain unreleased until other dependent instances are disposed?

